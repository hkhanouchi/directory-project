{% extends ':layout:base-layout.html.twig' %}

{% block title %}AnnuaireProjets - Clients{% endblock %}

{% block page_title %}Clients - {% endblock %}
{% block page_subtitle %}{{ 'customer.index.new_customer'|trans({}, 'messages') }}{% endblock %}

{% block page_content %}

    <div class="box box-primary">
            <div class="box-header with-border">
              <h3 class="box-title">{{ 'customer.index.new_customer'|trans({}, 'messages') }}</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
              {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Créer" class="btn btn-primary" />
              {{ form_end(form) }}
            </div>
            <!-- /.box-body -->
          </div>

{% endblock %}
