{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('app_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div class="pull-left">
        <input class="btn btn-primary btn-block" type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
{{ form_end(form) }}
